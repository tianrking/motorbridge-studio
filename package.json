{
  "name": "factory-calib-ui-ws",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:ready": "npm install --no-audit --no-fund && vite --host 0.0.0.0 --port 18110",
    "dev": "vite --host 0.0.0.0 --port 18110",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 18110"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
